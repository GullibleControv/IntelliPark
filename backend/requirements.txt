# Web Framework
flask==3.0.0
flask-cors==4.0.0
flask-sqlalchemy==3.1.1

# Authentication
pyjwt==2.8.0
bcrypt==4.1.2

# Environment
python-dotenv==1.0.0

# Database (PostgreSQL for production)
psycopg2-binary==2.9.9

# Validation
email-validator==2.1.0

# Production server
gunicorn==21.2.0

# Note: opencv-python-headless and yt-dlp removed for cloud deployment
# These can be added later for local admin panel features
